<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyXamarinApps.CoffeeEquipmentPage"
             xmlns:viewmodels="clr-namespace:MyXamarinApps.ViewModels">
    <ContentPage.BindingContext>
        <viewmodels:CoffeeEquipmentViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label FontSize="Large" Text="{Binding CountDisplay}" />
                <Button Text="Click Me" Command="{Binding IncreaseCount}" />
                <Button Text="Refresh" Command="{Binding CallServerCommand}" />
            </StackLayout>
            <ListView ItemsSource="{Binding Coffee}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>